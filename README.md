waow another portfolio website built in react

## Installation
`npm install` for dependencies \
`npm run dev` to run it locally \
crazy, I know 

One actual thing is that the visitor counter uses a personal server that I'm hosting from my own home, so it may not show up (why host it rather than using a free provider? because funi)

## Deployment
The website should automatically deploy through the work flow on pushes to main.
